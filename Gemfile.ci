#!/usr/bin/env bundle
# encoding: utf-8

source 'https://rubygems.org'

group :development do
  gem 'bundler', '~> 1.3.4'

  gem 'rake', '~> 10.0.4'
end

group :development, :test do
  gem 'rspec', '~> 2.13.0'
end